[build]
  publish = "site/"
  functions = "netlify/functions"

# Redirect rules (backup — _redirects in site/ takes priority)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
